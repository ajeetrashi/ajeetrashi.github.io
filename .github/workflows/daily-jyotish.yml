name: Daily Jyotish Push

# Sends Ajeet's daily reading at 7 AM Singapore time (23:00 UTC) via ntfy.
# NOTE: scheduled workflows only run from the DEFAULT branch (main), so this
# must be merged to main to fire on schedule. Use "Run workflow" to test.

on:
  schedule:
    - cron: "0 23 * * *"   # 23:00 UTC = 07:00 Asia/Singapore
  workflow_dispatch: {}      # manual test button in the Actions tab

permissions:
  contents: read

concurrency:
  group: daily-jyotish
  cancel-in-progress: false

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Send today's reading
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          NTFY_SERVER: ${{ secrets.NTFY_SERVER }}
        run: python scripts/daily_jyotish.py
